spring:
  application:
    name: profile
    
  datasource:
    url: jdbc:mysql://localhost:3306/social
    username: root
    password: root
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create

server:
   port: 9001
